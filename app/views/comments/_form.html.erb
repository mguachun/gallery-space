<%= render partial: "layouts/error", locals: {object: @comment}  %>

<%= form_for(comment, url: work_comments_path(comment.work, comment)) do |f| %>
    <%= f.label "Headline" %> 
    <%= f.text_field :headline%><br></br>
    <%= f.label "Description" %> 
    <%= f.text_field :description %><br></br>

    <%= f.submit %> 
<% end %>
